---
import "../../style/components/background/home-background.css";

interface Props {
  revealAt?: string;
  mode?: "light" | "dark";
  animated?: boolean;
}

const { revealAt = "12% 12%", mode, animated = true } = Astro.props satisfies Props;
const backgroundMode = mode === "dark" ? "dark" : undefined;
---

<div
  class="snow-bg"
  data-animated={animated ? "true" : "false"}
  data-mode={backgroundMode}
  aria-hidden="true"
  style={`--snow-reveal-at:${revealAt};`}
></div>

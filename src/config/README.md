# Config Directory

Store user-specific customization here.

## Current file

- `site.ts`: site identity, metadata, navigation, and social links.

## Rule

- UI components should read from this directory instead of hardcoding business content.
- For a different user, update only files in this directory.
